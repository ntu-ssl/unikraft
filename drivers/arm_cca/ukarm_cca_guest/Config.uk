config LIBUKARM_CCA_GUEST
	bool "Arm CCA guest"
	depends on ARCH_ARM_64
	select LIBUKRSI
	select LIBUKPAGING
	help
		Support for running in an Arm CCA realm.

if LIBUKARM_CCA_GUEST
config LIBUKARM_CCA_GUEST_TEST
	bool "Enable tests for CCA guest features"
	select LIBUKTEST
endif
