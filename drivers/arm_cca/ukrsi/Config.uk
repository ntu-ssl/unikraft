config LIBUKRSI
	bool "Realm Services Interface library"
	depends on ARCH_ARM_64
	depends on PLAT_KVM
	depends on KVM_BOOT_PROTO_QEMU_VIRT
	help
		Realm Services Interface (RSI) support.

if LIBUKRSI
config LIBUKRSI_TEST
	bool "Enable integration tests for RSI"
	select LIBUKTEST
endif
